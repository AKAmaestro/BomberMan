import type { GameConfig } from '../types/GameTypes';

export const DEFAULT_CONFIG: GameConfig = {
  initialGridSize: 15,
  levelTimeLimit: 300,
  maxHealth: 100,
  initialTNTCount: 3,
  explosionRadius: 2
}; 